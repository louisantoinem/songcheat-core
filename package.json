{
  "name": "songcheat-core",
  "version": "1.0.0",
  "description": "Base classes for handling songcheat files. Can be used both in Node and in browser.",
  "main": "dist/songcheat-core.umd.js",
  "module": "dist/songcheat-core.esm.js",
  "scripts": {
    "clean": "rm -f dist/*.js && rm -f samples/*.json && rm -f samples/*.lyrics && rm -f samples/*.log",
    "lint": "standard --fix src/*js *.js",
    "build": "rollup -c",
    "dev": "rollup -cw",
    "test": "node test",
    "cc": "node cc samples/*.txt dist/samples.js"
  },
  "keywords": [
    "songcheat",
    "music",
    "song",
    "scoresheet",
    "notation",
    "chords"
  ],
  "author": "Louis Antoine-Moussiaux",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "json-stringify-pretty-compact": "^1.0.4"
  }
}
